(ns {{{name}}}.core-test
  (:require [cemerick.cljs.test]
            [{{{name}}}.core])
  (:require-macros [cemerick.cljs.test :refer [is deftest testing with-test]]))

;; (deftest test-def
;;   (testing "test 1"
;;     (is (= 0 1))))
