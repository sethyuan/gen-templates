(ns {{{name}}}.core
  (:require [clojure.core.match :refer [match]]
            [cav.macros :refer :all]
            [cav.core :as cav])
  (:gen-class))

(defn -main [& args]
  (println x "Hello, World!"))
