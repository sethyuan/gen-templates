(ns {{{name}}}.core-test
  (:require [cemerick.cljs.test :refer-macros [is deftest testing with-test]]
            [{{{name}}}.core]))

;; (deftest test-def
;;   (testing "test 1"
;;     (is (= 0 1))))
