(ns {{{name}}}.core-test
  (:require [{{{name}}}.core :as core]
            [cemerick.cljs.test :refer-macros [deftest testing is with-test]]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
